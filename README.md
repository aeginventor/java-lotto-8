# 3주 차 로또 미션

## Phase 1: MVP 기능 목록
- [x] `Lotto` 클래스 유효성 검사
    - [x] 로또 번호 개수가 6개가 아닐 경우 예외 발생
    - [x] 로또 번호에 중복된 숫자가 있을 경우 예외 발생
    - [x] 로또 번호가 1~45 범위를 벗어날 경우 예외 발생
- [x] 구입 금액 입력
    - [x] 구입 금액이 숫자가 아닐 경우 예외 발생
    - [x] 구입 금액이 1,000원 단위가 아닐 경우 예외 발생
    - [x] 구입 금액이 1,000원 미만일 경우 예외 발생
- [x] 구입 금액만큼 로또 발행 기능
- [x] 발행된 로또 개수 및 번호(오름차순) 출력
- [x] 당첨 번호 입력
    - [x] 당첨 번호가 쉼표(,)로 구분되지 않거나, 숫자가 아닌 값을 포함할 경우 예외 발생
    - [x] 당첨 번호 개수가 6개가 아닐 경우 예외 발생
    - [x] 당첨 번호에 중복된 숫자가 있을 경우 예외 발생
    - [x] 당첨 번호가 1~45 범위를 벗어날 경우 예외 발생
- [x] 보너스 번호 입력
    - [x] 보너스 번호가 숫자가 아닐 경우 예외 발생
    - [x] 보너스 번호가 1~45 범위를 벗어날 경우 예외 발생
    - [x] 보너스 번호가 당첨 번호와 중복될 경우 예외 발생
- [x] 당첨 내역 계산 (등수별 몇 개인지)
- [x] 당첨 통계 출력 (수익률 포함)
- [x] 예외 발생 시 `[ERROR]` 메시지 출력 후, 해당 입력부터 다시 받기

## Phase 2: 리팩토링 및 개선
- [x] MVC 패턴 적용 (Application 클래스 책임 분리)
  - [x] `InputView`: 사용자 입력(UI) 책임 분리
  - [x] `OutputView`: 콘솔 출력(UI) 책임 분리
  - [x] `LottoGameController`: 게임 흐름 제어 및 예외 처리 책임 분리
- [x] 도메인/검증 로직 중복 제거
  - [x] `Lotto`와 `Validator` 간의 로또 번호 범위(1~45) 상수 및 로직 중복 제거
  - [x] `LottoService`, `Validator`에서 중복 상수 제거
  - [x] `Lotto`와 `Validator` 간의 `validateRange` 로직 및 에러 메시지 통합

## Phase 3: 최종 개선
- [x] 단일 책임 원칙(SRP) 점검 및 리팩토링
  - [x] Controller가 View의 책임을 가지던 문제(수익률 포맷팅) 해결
- [x] 패키지 구조 개선 (MVC 및 도메인 분리)
  - [x] `main` 소스 코드를 `controller`, `domain`, `service`, `util`, `view` 패키지로 분리
  - [x] `test` 소스 코드를 `main` 패키지 구조와 동일하게 분리
- [x] 추가 엣지 케이스
  - [x] `Validator`: 모든 입력에 대해 `null` 또는 빈 값("") 검증 추가
  - [x] `Validator`: 구매 금액 입력 시 앞뒤 공백(`trim`) 허용
  - [x] `Validator`: 당첨 번호 입력 시 쉼표(,)로 끝나는 경우 예외 처리